import { async, ComponentFixture } from '@angular/core/testing';
import { TheosContactComponent } from './theos-contact.component';

describe('TheosContactComponent', () => {
  let component: TheosContactComponent;
  let fixture: ComponentFixture<TheosContactComponent>;

  beforeEach(async(() => {
    // TestBed.configureTestingModule({
    //   declarations: [ TheosContactComponent ]
    // })
    // .compileComponents();
  }));

  beforeEach(() => {
    // fixture = TestBed.createComponent(TheosContactComponent);
    // component = fixture.componentInstance;
    // fixture.detectChanges();
  });

  it('should create', () => {
    // expect(component).toBeTruthy();
  });
});
