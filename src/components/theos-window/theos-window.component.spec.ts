import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { TheosWindowComponent } from './theos-window.component';

describe('TheosWindowComponent', () => {
  let component: TheosWindowComponent;
  let fixture: ComponentFixture<TheosWindowComponent>;

  beforeEach(async(() => {
    // TestBed.configureTestingModule({
    //   declarations: [ TheosWindowComponent ]
    // })
    // .compileComponents();
  }));

  beforeEach(() => {
    // fixture = TestBed.createComponent(TheosWindowComponent);
    // component = fixture.componentInstance;
    // fixture.detectChanges();
  });

  it('should create', () => {
    // expect(component).toBeTruthy();
  });
});
