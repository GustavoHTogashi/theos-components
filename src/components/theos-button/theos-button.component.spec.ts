import { async, ComponentFixture, TestBed } from '@angular/core/testing';
import { TheosButtonComponent } from './theos-button.component';


describe('TheosButtonsComponent', () => {
  let component: TheosButtonComponent;
  let fixture: ComponentFixture<TheosButtonComponent>;

  beforeEach(async(() => {
    // TestBed.configureTestingModule({
    //   declarations: [ TheosButtonComponent ]
    // })
    // .compileComponents();
  }));

  beforeEach(() => {
    // fixture = TestBed.createComponent(TheosButtonComponent);
    // component = fixture.componentInstance;
    // fixture.detectChanges();
  });

  it('should create', () => {
    // expect(component).toBeTruthy();
  });
});
