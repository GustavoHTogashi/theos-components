import { async, ComponentFixture, TestBed } from '@angular/core/testing';
import { TheosSearchDialogComponent } from './theos-search-dialog.component';

describe('TheosSearchDialogComponent', () => {
  let component: TheosSearchDialogComponent;
  let fixture: ComponentFixture<TheosSearchDialogComponent>;

  beforeEach(async(() => {
    // TestBed.configureTestingModule({
    //   declarations: [TheosSearchDialogComponent],
    // }).compileComponents();
  }));

  beforeEach(() => {
    // fixture = TestBed.createComponent(TheosSearchDialogComponent);
    // component = fixture.componentInstance;
    // fixture.detectChanges();
  });

  it('should create', () => {
    // expect(component).toBeTruthy();
  });
});
