import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { TheosInputSearchGroupComponent } from './theos-input-search-group.component';

describe('TheosInputSearchFullComponent', () => {
  let component: TheosInputSearchGroupComponent;
  let fixture: ComponentFixture<TheosInputSearchGroupComponent>;

  beforeEach(async(() => {
    // TestBed.configureTestingModule({
    //   declarations: [ TheosInputSearchGroupComponent ]
    // })
    // .compileComponents();
  }));

  beforeEach(() => {
    // fixture = TestBed.createComponent(TheosInputSearchGroupComponent);
    // component = fixture.componentInstance;
    // fixture.detectChanges();
  });

  it('should create', () => {
    // expect(component).toBeTruthy();
  });
});
