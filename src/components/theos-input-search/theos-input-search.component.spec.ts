import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { TheosInputSearchComponent } from './theos-input-search.component';

describe('TheosInputSearchComponent', () => {
  let component: TheosInputSearchComponent;
  let fixture: ComponentFixture<TheosInputSearchComponent>;

  beforeEach(async(() => {
    // TestBed.configureTestingModule({
    //   declarations: [ TheosInputSearchComponent ]
    // })
    // .compileComponents();
  }));

  beforeEach(() => {
    // fixture = TestBed.createComponent(TheosInputSearchComponent);
    // component = fixture.componentInstance;
    // fixture.detectChanges();
  });

  it('should create', () => {
    // expect(component).toBeTruthy();
  });
});
