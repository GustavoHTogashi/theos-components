import { async, ComponentFixture } from '@angular/core/testing';
import { TheosAddressComponent } from './theos-address.component';

describe('TheosAddressComponent', () => {
  let component: TheosAddressComponent;
  let fixture: ComponentFixture<TheosAddressComponent>;

  beforeEach(async(() => {
    // TestBed.configureTestingModule({
    //   declarations: [ TheosAddressComponent ]
    // })
    // .compileComponents();
  }));

  beforeEach(() => {
    // fixture = TestBed.createComponent(TheosAddressComponent);
    // component = fixture.componentInstance;
    // fixture.detectChanges();
  });

  it('should create', () => {
    // expect(component).toBeTruthy();
  });
});
