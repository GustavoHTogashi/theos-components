import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { TheosPaginationComponent } from './theos-pagination.component';

describe('TheosPaginationComponent', () => {
  let component: TheosPaginationComponent;
  let fixture: ComponentFixture<TheosPaginationComponent>;

  beforeEach(async(() => {
    // TestBed.configureTestingModule({
    //   declarations: [ TheosPaginationComponent ]
    // })
    // .compileComponents();
  }));

  beforeEach(() => {
    // fixture = TestBed.createComponent(TheosPaginationComponent);
    // component = fixture.componentInstance;
    // fixture.detectChanges();
  });

  it('should create', () => {
    // expect(component).toBeTruthy();
  });
});
