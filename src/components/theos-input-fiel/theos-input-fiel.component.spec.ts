import { async, ComponentFixture } from '@angular/core/testing';
import { TheostheosInputFielComponent } from './theos-input-fiel.component';

describe('TheosContactComponent', () => {
  let component: TheostheosInputFielComponent;
  let fixture: ComponentFixture<TheostheosInputFielComponent>;

  beforeEach(async(() => {
    // TestBed.configureTestingModule({
    //   declarations: [ TheostheosInputFielComponent ]
    // })
    // .compileComponents();
  }));

  beforeEach(() => {
    // fixture = TestBed.createComponent(TheostheosInputFielComponent);
    // component = fixture.componentInstance;
    // fixture.detectChanges();
  });

  it('should create', () => {
    // expect(component).toBeTruthy();
  });
});
