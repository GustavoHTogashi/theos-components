import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { TheosCalendarComponent } from './theos-calendar.component';

describe('TheosCalendarComponent', () => {
  let component: TheosCalendarComponent;
  let fixture: ComponentFixture<TheosCalendarComponent>;

  beforeEach(async(() => {
    // TestBed.configureTestingModule({
    //   declarations: [ TheosCalendarComponent ]
    // })
    // .compileComponents();
  }));

  beforeEach(() => {
    // fixture = TestBed.createComponent(TheosCalendarComponent);
    // component = fixture.componentInstance;
    // fixture.detectChanges();
  });

  it('should create', () => {
    // expect(component).toBeTruthy();
  });
});
